@section BannerSection{
    @{Html.RenderAction("RenderSlide", "Home");}
}
@section SearchSection{
    @{ Html.RenderPartial("~/Views/Shared/_PartialAutoComplateSearch.cshtml");}
 }
@section BodySection{
    <div class="row">
        <div class="col-md-9 mt-2">
            @{Html.RenderAction("SaleProduct", "Home");}
            @{Html.RenderAction("TrendingProduct", "Home");}
        </div>
        <div class="col-md-3 mt-2">
            @{ Html.RenderAction("GetLastArticle", "Home");}
            @{ Html.RenderAction("GetLastNews", "Home");}
            @{ Html.RenderAction("GetLastEvents", "Home");}
        </div>
    </div>
}